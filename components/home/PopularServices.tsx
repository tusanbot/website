"use client";
import{useEffect,useRef,useState}from"react";import Link from"next/link";import{motion,useAnimationFrame,useMotionValue}from"framer-motion";import{supabase}from"@/lib/supabase";import{SectionHeader}from"@/components/ui";
type Service={id:string;title:string;slug:string|null;category:string|null;description:string|null;price:number};export default function PopularServices(){const[services,setServices]=useState<Service[]>([]),[selected,setSelected]=useState<string|null>(null);const railRef=useRef<HTMLDivElement>(null),x=useMotionValue(0),[travel,setTravel]=useState(0);useEffect(()=>{supabase.from("services").select("id,title,slug,category,description,price").eq("is_active",true).order("is_popular",{ascending:false}).order("created_at",{ascending:false}).limit(10).then(({data})=>setServices((data||[])as Service[]))},[]);useEffect(()=>{const f=()=>setTravel(railRef.current?.scrollWidth?railRef.current.scrollWidth/2:0);f();window.addEventListener("resize",f);return()=>window.removeEventListener("resize",f)},[services]);useAnimationFrame((_,d)=>{if(selected||!travel)return;const n=x.get()+d*.035;x.set(n>travel?n-travel:n)});return <section id="popular-services" className="relative scroll-mt-28 py-7 sm:py-9" dir="rtl" onClick={e=>{if(!(e.target as HTMLElement).closest("[data-service-tag]"))setSelected(null)}}><div className="mx-auto max-w-7xl px-4 lg:px-8"><SectionHeader title="خدمات محبوب توسن" description="روی هر خدمت بزنید تا توضیحات آن را ببینید." align="center"/><div className="relative mt-5 w-full overflow-hidden rounded-3xl border border-[var(--border)] bg-[var(--surface)]/70 py-3 shadow-sm"><motion.div ref={railRef} style={{x}} className="flex w-max gap-3 px-3" dir="ltr">{[...services,...services].map((s,i)=>{const active=selected===s.id;const href=s.slug?`/services/${encodeURIComponent(s.slug)}`:`/services?category=${encodeURIComponent(s.category||"all")}`;return <div key={`${s.id}-${i}`} data-service-tag className="relative shrink-0" dir="rtl"><button type="button" onClick={()=>setSelected(v=>v===s.id?null:s.id)} className={`rounded-full border px-5 py-2.5 text-sm font-black whitespace-nowrap ${active?"border-[var(--primary)] bg-[var(--primary)] text-white shadow-lg":"border-[var(--border)] bg-[var(--surface-secondary)] text-[var(--text)]"}`}><span>{s.title}</span>{s.price>0&&<span className={`mr-2 ${active?"text-white/85":"text-[var(--primary)]"}`}>{s.price.toLocaleString("fa-IR")} تومان</span>}</button>{active&&<motion.div initial={{opacity:0,y:-6,height:0}} animate={{opacity:1,y:0,height:"auto"}} className="absolute right-0 top-full z-30 mt-2 w-[min(330px,calc(100vw-32px))] overflow-hidden rounded-2xl border border-[var(--primary)]/20 bg-[var(--surface)] p-4 text-right shadow-2xl"><p className="text-sm leading-7 text-[var(--text-muted)]">{s.description||"توضیحات این خدمت در دسترس است."}</p><Link href={href} className="mt-3 block rounded-xl bg-[var(--primary)] px-4 py-2 text-center text-sm font-black text-white">مشاهده و ثبت سفارش</Link></motion.div>}</div>})}</motion.div></div></div></section>}
