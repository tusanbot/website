"use client";

import { ChangeEvent, ReactNode, useEffect, useRef, useState } from "react";
import { CheckCircle2, Download, Film, Loader2, Maximize2, RotateCcw, Scissors, Subtitles, Upload, Volume2, VolumeX } from "lucide-react";
import { makeBrowserPlayable, convertVideoFormat } from "@/lib/video/ffmpeg";

type Meta = { duration: number; width: number; height: number; size: number; type: string };
type Crop = { x: number; y: number; width: number; height: number };
type SubtitleCue = { start: number; end: number; text: string };
type VideoWithFrameCallback = HTMLVideoElement & { requestVideoFrameCallback?: (callback: (now: number, metadata: { mediaTime: number }) => void) => number; cancelVideoFrameCallback?: (handle: number) => void };
const MB = 1024 * 1024;

function formatTime(seconds: number) { if (!Number.isFinite(seconds)) return "00:00"; const s = Math.max(0, Math.floor(seconds)); return `${Math.floor(s / 60).toString().padStart(2, "0")}:${(s % 60).toString().padStart(2, "0")}`; }
function download(blob: Blob, name: string) { const url = URL.createObjectURL(blob); const a = document.createElement("a"); a.href = url; a.download = name; document.body.appendChild(a); a.click(); a.remove(); setTimeout(() => URL.revokeObjectURL(url), 2000); }
function downloadUrl(url: string, name: string) { const a = document.createElement("a"); a.href = url; a.download = name; document.body.appendChild(a); a.click(); a.remove(); }
function parseTime(value: string) { const p = value.replace(",", ".").split(":").map(Number); if (p.length === 3) return p[0] * 3600 + p[1] * 60 + p[2]; if (p.length === 2) return p[0] * 60 + p[1]; return Number(p[0]) || 0; }
function parseSubtitles(text: string): SubtitleCue[] { const normalized = text.replace(/\r/g, "").trim(); if (!normalized) return []; return normalized.split(/\n\s*\n/).flatMap((block) => { const lines = block.split("\n").map((x) => x.trim()).filter(Boolean); const i = lines.findIndex((x) => x.includes(" --> ")); if (i < 0) return []; const [a, b] = lines[i].split(" --> "); const start = parseTime(a); const end = parseTime(b.split(" ")[0]); const cueText = lines.slice(i + 1).join("\n"); return end > start && cueText ? [{ start, end, text: cueText }] : []; }); }
function isVideoFile(file: File) { return file.type.startsWith("video/") || /\.(mp4|webm|mov|m4v|avi|mkv|mpeg|mpg|ogv|3gp|ts|mts|m2ts)$/i.test(file.name); }
function pickMime(format: "webm" | "mp4") { const list = format === "mp4" ? ["video/mp4;codecs=avc1.42E01E,mp4a.40.2", "video/mp4"] : ["video/webm;codecs=vp9,opus", "video/webm;codecs=vp8,opus", "video/webm"]; return list.find((value) => typeof MediaRecorder !== "undefined" && MediaRecorder.isTypeSupported(value)) || ""; }

export default function VideoManager() {
  const inputRef = useRef<HTMLInputElement>(null); const videoRef = useRef<HTMLVideoElement>(null); const canvasRef = useRef<HTMLCanvasElement>(null);
  const sourceUrlRef = useRef(""); const outputUrlRef = useRef(""); const thumbnailUrlRef = useRef("");
  const [file, setFile] = useState<File | null>(null); const [sourceUrl, setSourceUrl] = useState(""); const [meta, setMeta] = useState<Meta | null>(null); const [output, setOutput] = useState<{ url: string; name: string; size: number } | null>(null); const [thumbnail, setThumbnail] = useState("");
  const [busy, setBusy] = useState(false); const [progress, setProgress] = useState(0); const [message, setMessage] = useState(""); const [operation, setOperation] = useState("آماده");
  const [start, setStart] = useState(0); const [end, setEnd] = useState(0); const [width, setWidth] = useState(0); const [height, setHeight] = useState(0); const [quality, setQuality] = useState(70); const [fps, setFps] = useState(30); const [format, setFormat] = useState<"webm" | "mp4">("webm"); const [muted, setMuted] = useState(false);
  const [cropEnabled, setCropEnabled] = useState(false); const [crop, setCrop] = useState<Crop>({ x: 0, y: 0, width: 1, height: 1 }); const [subtitleText, setSubtitleText] = useState(""); const [subtitles, setSubtitles] = useState<SubtitleCue[]>([]); const [thumbnailTime, setThumbnailTime] = useState(0); const [codecFallback, setCodecFallback] = useState(false);
  const duration = meta?.duration || 0;

  useEffect(() => () => { [sourceUrlRef, outputUrlRef, thumbnailUrlRef].forEach((r) => { if (r.current) URL.revokeObjectURL(r.current); }); }, []);
  const applyMetadata = (v: HTMLVideoElement, selected: File) => { if (v.videoWidth <= 0 || v.videoHeight <= 0 || !Number.isFinite(v.duration)) return; const next = { duration: v.duration, width: v.videoWidth, height: v.videoHeight, size: selected.size, type: selected.type || "video/" }; setMeta(next); setStart(0); setEnd(next.duration); setWidth(next.width); setHeight(next.height); setThumbnailTime(Math.max(0, next.duration / 2)); setOperation("آماده"); setMessage(codecFallback ? "کدک با موفقیت سازگار شد." : "ویدیو آماده پردازش است."); };
  const select = (e: ChangeEvent<HTMLInputElement>) => { const next = e.target.files?.[0]; if (!next) return; if (!isVideoFile(next)) { setMessage("فقط فایل ویدیویی انتخاب کنید."); return; } if (sourceUrlRef.current) URL.revokeObjectURL(sourceUrlRef.current); const url = URL.createObjectURL(next); sourceUrlRef.current = url; setFile(next); setSourceUrl(url); setMeta(null); setOutput(null); setThumbnail(""); setCodecFallback(false); setMessage("در حال آماده‌سازی ویدیو و خواندن مشخصات فایل..."); setOperation("در حال آماده‌سازی"); };
  const failed = async () => { if (!file || codecFallback || busy) return; setCodecFallback(true); setBusy(true); setProgress(0); setOperation("در حال سازگار کردن کدک"); setMessage("در حال تبدیل موقت ویدیو با FFmpeg..."); try { const playable = await makeBrowserPlayable(file, (p) => setProgress(Math.round(p * 100))); if (sourceUrlRef.current) URL.revokeObjectURL(sourceUrlRef.current); const url = URL.createObjectURL(playable); sourceUrlRef.current = url; setFile(playable); setSourceUrl(url); setMeta(null); setProgress(0); setOperation("در حال آماده‌سازی"); } catch (err) { setOperation("خطا"); setMessage(err instanceof Error ? `تبدیل کدک انجام نشد: ${err.message}` : "تبدیل کدک انجام نشد."); } finally { setBusy(false); } };
  const seek = async (time: number) => { const v = videoRef.current; if (!v) return; const target = Math.max(0, Math.min(time, duration)); if (Math.abs(v.currentTime - target) < 0.02) return; await new Promise<void>((resolve, reject) => { const timer = window.setTimeout(() => { v.removeEventListener("seeked", done); reject(new Error("جابجایی به زمان انتخاب‌شده انجام نشد.")); }, 8000); const done = () => { window.clearTimeout(timer); v.removeEventListener("seeked", done); resolve(); }; v.addEventListener("seeked", done); v.currentTime = target; }); };
  const capture = async () => { const v = videoRef.current; const canvas = canvasRef.current; if (!v || !canvas || !meta) return; setBusy(true); setOperation("استخراج تصویر شاخص"); try { await seek(thumbnailTime); canvas.width = meta.width; canvas.height = meta.height; const ctx = canvas.getContext("2d"); if (!ctx) throw new Error("Canvas در این مرورگر در دسترس نیست."); ctx.drawImage(v, 0, 0, meta.width, meta.height); const blob = await new Promise<Blob>((resolve, reject) => canvas.toBlob((b) => b ? resolve(b) : reject(new Error("استخراج فریم ناموفق بود.")), "image/jpeg", 0.92)); if (thumbnailUrlRef.current) URL.revokeObjectURL(thumbnailUrlRef.current); thumbnailUrlRef.current = URL.createObjectURL(blob); setThumbnail(thumbnailUrlRef.current); download(blob, "tusan-video-thumbnail.jpg"); setMessage("تصویر شاخص با موفقیت استخراج شد."); } catch (err) { setMessage(err instanceof Error ? err.message : "استخراج فریم انجام نشد."); } finally { setBusy(false); setOperation("آماده"); } };
  const processVideo = async () => { const v = videoRef.current; const canvas = canvasRef.current; if (!v || !canvas || !meta || !file) return; const mimeType = pickMime(format); if (!mimeType) { setMessage(format === "mp4" ? "این مرورگر خروجی MP4 را مستقیم پشتیبانی نمی‌کند." : "خروجی WebM در این مرورگر پشتیبانی نمی‌شود."); return; } const outW = Math.max(2, Math.round(width / 2) * 2); const outH = Math.max(2, Math.round(height / 2) * 2); const from = Math.max(0, Math.min(start, duration)); const to = Math.min(duration, Math.max(from + 0.05, end || duration)); setBusy(true); setProgress(0); setOperation("در حال پردازش ویدیو"); try { canvas.width = outW; canvas.height = outH; const ctx = canvas.getContext("2d"); if (!ctx || typeof canvas.captureStream !== "function") throw new Error("پردازش ویدیویی Canvas در این مرورگر پشتیبانی نمی‌شود."); const stream = canvas.captureStream(fps); const videoMedia = v as HTMLVideoElement & { captureStream?: () => MediaStream }; if (!muted && typeof videoMedia.captureStream === "function") videoMedia.captureStream().getAudioTracks().forEach((track: MediaStreamTrack) => stream.addTrack(track)); const recorder = new MediaRecorder(stream, { mimeType: mimeType, videoBitsPerSecond: Math.max(250000, Math.round(4000000 * (quality / 100) ** 1.7)) }); const chunks: BlobPart[] = []; recorder.ondataavailable = (ev: BlobEvent) => { if (ev.data.size) chunks.push(ev.data); }; const stopped = new Promise<void>((resolve) => { recorder.onstop = () => resolve(); }); const draw = () => { const sx = cropEnabled ? crop.x * meta.width : 0; const sy = cropEnabled ? crop.y * meta.height : 0; const sw = cropEnabled ? crop.width * meta.width : meta.width; const sh = cropEnabled ? crop.height * meta.height : meta.height; ctx.clearRect(0, 0, outW, outH); ctx.drawImage(v, sx, sy, sw, sh, 0, 0, outW, outH); const cue = subtitles.find((x: SubtitleCue) => v.currentTime >= x.start && v.currentTime <= x.end); if (cue) { const lines = cue.text.split("\n"); ctx.font = `${Math.max(18, Math.round(outW / 38))}px Vazirmatn, sans-serif`; ctx.textAlign = "center"; ctx.textBaseline = "bottom"; lines.forEach((line: string, i: number) => { const y = outH - 35 - (lines.length - 1 - i) * Math.max(28, outH / 22); const m = ctx.measureText(line); ctx.fillStyle = "rgba(0,0,0,.72)"; ctx.fillRect((outW - m.width) / 2 - 14, y - 30, m.width + 28, 36); ctx.fillStyle = "#fff"; ctx.fillText(line, outW / 2, y); }); } }; const finish = () => { if (v.currentTime >= to - 0.04) { v.pause(); if (recorder.state === "recording") recorder.stop(); return true; } return false; }; const vv = v as VideoWithFrameCallback; const render = () => { if (recorder.state !== "recording") return; draw(); setProgress(Math.min(96, Math.round(((v.currentTime - from) / Math.max(0.05, to - from)) * 96))); if (!finish()) requestAnimationFrame(render); }; const onFrame = (_now: number, metadata: { mediaTime: number }) => { if (recorder.state !== "recording") return; draw(); setProgress(Math.min(96, Math.round(((metadata.mediaTime - from) / Math.max(0.05, to - from)) * 96))); if (!finish() && vv.requestVideoFrameCallback) vv.requestVideoFrameCallback(onFrame); }; recorder.start(250); await seek(from); await v.play(); if (typeof vv.requestVideoFrameCallback === "function") vv.requestVideoFrameCallback(onFrame); else requestAnimationFrame(render); await new Promise<void>((resolve) => { const timer = window.setInterval(() => { if (recorder.state === "inactive") { window.clearInterval(timer); resolve(); } }, 100); }); await stopped; stream.getTracks().forEach((track: MediaStreamTrack) => track.stop()); const raw = new Blob(chunks, { type: mimeType }); if (!raw.size) throw new Error("خروجی خالی تولید شد."); let finalFile = new File([raw], `tusan-video-${Date.now()}.webm`, { type: "video/webm" }); if (format === "mp4") { setOperation("در حال تبدیل به MP4"); finalFile = await convertVideoFormat(finalFile, "mp4", (p) => setProgress(96 + Math.round(p * 4))); } if (outputUrlRef.current) URL.revokeObjectURL(outputUrlRef.current); outputUrlRef.current = URL.createObjectURL(finalFile); setOutput({ url: outputUrlRef.current, name: finalFile.name, size: finalFile.size }); setProgress(100); setOperation("تمام شد"); setMessage(`خروجی ${format.toUpperCase()} آماده است؛ حجم ${(finalFile.size / MB).toFixed(2)} MB.`); } catch (err) { setOperation("خطا"); setMessage(err instanceof Error ? err.message : "پردازش ویدیو ناموفق بود."); } finally { setBusy(false); } };
  const loadSubtitles = (e: ChangeEvent<HTMLInputElement>) => { const f = e.target.files?.[0]; if (!f) return; const reader = new FileReader(); reader.onload = () => { const text = String(reader.result || ""); setSubtitleText(text); setSubtitles(parseSubtitles(text)); }; reader.readAsText(f); };
  const reset = () => { [sourceUrlRef, outputUrlRef, thumbnailUrlRef].forEach((r) => { if (r.current) URL.revokeObjectURL(r.current); r.current = ""; }); setFile(null); setSourceUrl(""); setMeta(null); setOutput(null); setThumbnail(""); setMessage(""); setOperation("آماده"); setProgress(0); if (inputRef.current) inputRef.current.value = ""; };

  return <div className="rounded-[2rem] border border-[var(--border)] bg-[var(--surface)] p-5 shadow-sm sm:p-7" dir="rtl">