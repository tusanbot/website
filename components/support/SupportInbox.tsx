"use client";

import { useEffect, useMemo, useState } from "react";
import { MessageCircle, Send, CheckCircle, ShoppingBag } from "lucide-react";
import { GlassPanel, SectionHeader } from "@/components/ui";
import { supabase } from "@/lib/supabase";

type Conversation = { id: string; user_id: string; status: "open" | "closed"; created_at: string; updated_at: string; order_id?: string | null; assignment_mode?: string | null; assigned_staff_id?: string | null; profiles?: { full_name?: string | null; phone?: string | null } | null; orders?: { id:string; tracking_code:string|null; status:string|null } | null };
type Message = { id: string; conversation_id: string; sender_id: string; sender_role: "user" | "admin" | "staff"; message: string; is_read: boolean; created_at: string };
const PAGE_SIZE = 50;
export default function SupportInbox({ backHref, backLabel = "بازگشت به پنل ←" }: { backHref?: string; backLabel?: string }) {
 const [conversations,setConversations]=useState<Conversation[]>([]),[unreadCounts,setUnreadCounts]=useState<Record<string,number>>({}),[selected,setSelected]=useState<string|null>(null),[messages,setMessages]=useState<Message[]>([]),[draft,setDraft]=useState(""),[loading,setLoading]=useState(true),[messagesLoading,setMessagesLoading]=useState(false),[error,setError]=useState<string|null>(null),[sending,setSending]=useState(false),[closing,setClosing]=useState(false),[hasMore,setHasMore]=useState(false),[oldestAt,setOldestAt]=useState<string|null>(null);
 useEffect(()=>{let mounted=true;async function init(){const {data:{user}}=await supabase.auth.getUser();if(!mounted)return;if(!user){setError("دسترسی به حساب کاربری امکان‌پذیر نیست.");setLoading(false);return}await loadConversations()}void init();const channel=supabase.channel("support-inbox-list").on("postgres_changes",{event:"*",schema:"public",table:"support_conversations"},()=>void loadConversations()).on("postgres_changes",{event:"INSERT",schema:"public",table:"support_messages"},p=>{const n=p.new as Message;void refreshUnreadCount(n.conversation_id)}).subscribe();return()=>{mounted=false;void supabase.removeChannel(channel)}},[]);
 useEffect(()=>{if(!selected)return;let mounted=true;const channel=supabase.channel(`support-inbox-${selected}`).on("postgres_changes",{event:"INSERT",schema:"public",table:"support_messages",filter:`conversation_id=eq.${selected}`},p=>{if(!mounted)return;const n=p.new as Message;setMessages(c=>c.some(x=>x.id===n.id)?c:[...c,n]);if(n.sender_role==="user")void markConversationRead(selected)}).subscribe();void loadMessages(selected);return()=>{mounted=false;void supabase.removeChannel(channel)}},[selected]);
 async function refreshUnreadCount(id:string){const {data,error:e}=await supabase.rpc("get_support_unread_count",{p_conversation_id:id});if(!e&&typeof data==="number")setUnreadCounts(c=>({...c,[id]:data}))}
 async function markConversationRead(id:string){const {error:e}=await supabase.rpc("mark_support_messages_read",{p_conversation_id:id});if(e){console.error(e);setError("پیام‌ها نمایش داده شدند، اما وضعیت خوانده‌شدن به‌روزرسانی نشد.");return}setUnreadCounts(c=>({...c,[id]:0}))}
 async function loadConversations(){const {data,error:e}=await supabase.from("support_conversations").select("id,user_id,status,created_at,updated_at,order_id,assignment_mode,assigned_staff_id,profiles!support_conversations_user_id_fkey(full_name,phone),orders!support_conversations_order_id_fkey(id,tracking_code,status)").eq("status","open").order("updated_at",{ascending:false});if(e){console.error(e);setError("بارگذاری گفتگوها انجام نشد. لطفاً دوباره تلاش کنید.");setLoading(false);return}const next=(data||[]) as Conversation[];setConversations(next);setSelected(c=>c&&next.some(x=>x.id===c)?c:next[0]?.id??null);setLoading(false);await Promise.all(next.map(x=>refreshUnreadCount(x.id)))}
 async function loadMessages(id:string){setMessagesLoading(true);setError(null);const {data,error:e}=await supabase.from("support_messages").select("id,conversation_id,sender_id,sender_role,message,is_read,created_at").eq("conversation_id",id).order("created_at",{ascending:false}).limit(PAGE_SIZE+1);if(e){console.error(e);setError("بارگذاری پیام‌ها انجام نشد. لطفاً دوباره تلاش کنید.");setMessagesLoading(false);return}const rows=(data||[]) as Message[];const page=rows.slice(0,PAGE_SIZE).reverse();setMessages(page);setHasMore(rows.length>PAGE_SIZE);setOldestAt(page[0]?.created_at??null);setMessagesLoading(false);await markConversationRead(id)}
 async function loadOlderMessages(){if(!selected||!oldestAt||!hasMore||messagesLoading)return;setMessagesLoading(true);const {data,error:e}=await supabase.from("support_messages").select("id,conversation_id,sender_id,sender_role,message,is_read,created_at").eq("conversation_id",selected).lt("created_at",oldestAt).order("created_at",{ascending:false}).limit(PAGE_SIZE+1);if(e){setError("بارگذاری پیام‌های قدیمی انجام نشد.");setMessagesLoading(false);return}const rows=(data||[]) as Message[];const page=rows.slice(0,PAGE_SIZE).reverse();setMessages(c=>[...page,...c.filter(x=>!page.some(o=>o.id===x.id))]);setHasMore(rows.length>PAGE_SIZE);setOldestAt(page[0]?.created_at??oldestAt);setMessagesLoading(false)}
 async function sendMessage(event:React.FormEvent){event.preventDefault();const text=draft.trim();if(!text||!selected||sending)return;setSending(true);setError(null);const {data:messageId,error:e}=await supabase.rpc("send_support_message",{p_conversation_id:selected,p_message:text});if(e||!messageId){console.error(e);setError(e?.message||"ارسال پیام انجام نشد. لطفاً دوباره تلاش کنید.");setSending(false);return}const {data:message,error:fe}=await supabase.from("support_messages").select("id,conversation_id,sender_id,sender_role,message,is_read,created_at").eq("id",messageId).maybeSingle();if(fe||!message)setError("پیام ارسال شد، اما بارگذاری آن در پنل انجام نشد.");else{setDraft("");setMessages(c=>c.some(x=>x.id===message.id)?c:[...c,message as Message])}setSending(false)}
 async function closeConversation(){if(!selected||closing)return;setClosing(true);setError(null);const {error:e}=await supabase.rpc("close_support_conversation",{p_conversation_id:selected});if(e){console.error(e);setError(e.message);setClosing(false);return}setConversations(c=>c.filter(x=>x.id!==selected));setSelected(null);setMessages([]);setClosing(false)}
 const active=useMemo(()=>conversations.find(x=>x.id===selected)||null,[conversations,selected]);
 const routing=active?.order_id?(active.assigned_staff_id?"مدیر سفارش":"صف اپراتور پشتیبانی"):"پشتیبانی عمومی";
 return <main dir="rtl" className="min-h-screen page-background p-4 sm:p-6 text-[var(--text)]"><div className="mx-auto max-w-7xl space-y-6"><div className="flex items-center justify-between gap-4"><SectionHeader title="پشتیبانی آنلاین" description="مدیریت گفت‌وگوهای زنده مشتریان با اپراتور توسن" />{backHref&&<a href={backHref} className="text-sm font-bold text-[var(--primary)]">{backLabel}</a>}</div>{error&&<div role="alert" className="rounded-xl border border-red-500/20 bg-red-500/10 px-4 py-3 text-sm">{error}</div>}<div className="grid gap-5 lg:grid-cols-[340px_1fr]"><GlassPanel className="overflow-hidden p-0"><div className="border-b border-[var(--border)] p-4 font-black">گفتگوهای باز</div><div className="max-h-[680px] overflow-y-auto">{loading?<div className="p-6 text-center text-sm text-[var(--text-muted)]">در حال بارگذاری...</div>:conversations.length===0?<div className="p-8 text-center text-sm leading-7 text-[var(--text-muted)]">گفتگوی بازی وجود ندارد.</div>:conversations.map(item=><button key={item.id} type="button" onClick={()=>setSelected(item.id)} className={`w-full border-b border-[var(--border)] p-4 text-right transition ${selected===item.id?"bg-[var(--primary)]/8":"hover:bg-[var(--surface-muted)]"}`}><div className="flex items-center justify-between gap-2"><div className="font-black">{item.profiles?.full_name||item.profiles?.phone||"کاربر"}</div>{(unreadCounts[item.id]||0)>0&&<span className="min-w-6 rounded-full bg-red-500 px-2 py-0.5 text-center text-[10px] font-black text-white">{unreadCounts[item.id]>99?"99+":unreadCounts[item.id]}</span>}</div><div className="mt-1 text-xs text-[var(--text-muted)]">{item.order_id?`سفارش: ${item.orders?.tracking_code||item.order_id.slice(0,8)}`:"گفتگوی عمومی"}</div><div className="mt-2 text-xs text-[var(--text-muted)]">{new Date(item.updated_at).toLocaleString("fa-IR")}</div></button>)}</div></GlassPanel><GlassPanel className="flex min-h-[680px] flex-col overflow-hidden p-0"><div className="border-b border-[var(--border)] p-4"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[var(--primary)]/10 text-[var(--primary)]"><MessageCircle size={20}/></span><div className="min-w-0 flex-1"><div className="font-black">{active?.profiles?.full_name||active?.profiles?.phone||"انتخاب گفتگو"}</div><div className="text-xs text-[var(--text-muted)]">{active?.profiles?.phone||""}</div></div>{selected&&<button type="button" onClick={()=>void closeConversation()} disabled={closing} className="flex items-center gap-2 rounded-xl border px-3 py-2 text-xs font-bold disabled:opacity-50"><CheckCircle size={16}/>{closing?"در حال اتمام...":"اتمام گفتگو"}</button>}</div>{active&&<div className="mt-4 grid gap-2 sm:grid-cols-3"><div className="rounded-xl border border-[var(--border)] bg-[var(--surface)] p-3 text-xs"><div className="text-[var(--text-muted)]">مسیر</div><div className="mt-1 font-black">{routing}</div></div><div className="rounded-xl border border-[var(--border)] bg-[var(--surface)] p-3 text-xs"><div className="text-[var(--text-muted)]">سفارش</div><div className="mt-1 flex items-center gap-1 font-black">{active.order_id?<><ShoppingBag size={14}/>{active.orders?.tracking_code||active.order_id.slice(0,8)}</>:"بدون سفارش"}</div></div><div className="rounded-xl border border-[var(--border)] bg-[var(--surface)] p-3 text-xs"><div className="text-[var(--text-muted)]">وضعیت سفارش</div><div className="mt-1 font-black">{active.orders?.status||"—"}</div></div></div>}{active?.order_id&&!active.assigned_staff_id&&<div className="mt-3 rounded-xl border border-amber-500/20 bg-amber-500/10 px-3 py-2 text-xs leading-6">مدیر سفارش هنوز برای این سفارش تعیین نشده است؛ این گفتگو در صف اپراتور پشتیبانی قرار دارد.</div>}</div><div className="flex-1 space-y-3 overflow-y-auto p-5">{selected&&hasMore&&<button type="button" onClick={()=>void loadOlderMessages()} disabled={messagesLoading} className="mx-auto block text-xs font-bold text-[var(--primary)] disabled:opacity-50">{messagesLoading?"در حال بارگذاری...":"نمایش پیام‌های قدیمی‌تر"}</button>}{!selected?<div className="flex h-full items-center justify-center text-[var(--text-muted)]">یک گفتگو را انتخاب کنید.</div>:messagesLoading&&messages.length===0?<div className="flex h-full items-center justify-center text-[var(--text-muted)]">در حال بارگذاری پیام‌ها...</div>:messages.map(item=><div key={item.id} className={`flex ${item.sender_role==="admin"||item.sender_role==="staff"?"justify-end":"justify-start"}`}><div className={`max-w-[75%] rounded-2xl px-4 py-3 leading-7 ${item.sender_role==="admin"||item.sender_role==="staff"?"bg-[var(--primary)] text-white":"bg-[var(--surface-muted)] text-[var(--text)]"}`}>{item.message}<div className={`mt-1 text-[10px] ${item.sender_role==="admin"||item.sender_role==="staff"?"text-white/60":"text-[var(--text-muted)]"}`}>{new Date(item.created_at).toLocaleTimeString("fa-IR",{hour:"2-digit",minute:"2-digit"})}</div></div></div>)}</div>{selected&&<form onSubmit={sendMessage} className="border-t border-[var(--border)] p-3"><div className="flex items-end gap-2 rounded-2xl border border-[var(--border)] p-2"><textarea value={draft} onChange={e=>setDraft(e.target.value)} rows={2} maxLength={4000} placeholder="پاسخ اپراتور..." className="flex-1 resize-none bg-transparent px-2 py-2 outline-none"/><button type="submit" disabled={!draft.trim()||sending} className="flex h-10 w-10 items-center justify-center rounded-xl bg-[var(--primary)] text-white disabled:opacity-40"><Send size={17}/></button></div></form>}</GlassPanel></div></div></main>;
}
