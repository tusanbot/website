"use client";

import { useState } from "react";
import type { ServiceSeoContentData } from "@/lib/services/seoContent";
import { TusanButton, TusanInput, TusanCard } from "@/components/ui";

export const emptyServiceSeoContent = (): ServiceSeoContentData => ({ intro: "", body: "", steps: [], requirements: [], notes: [], faq: [] });
function ListEditor({ label, items, onChange, placeholder }: { label: string; items: string[]; onChange: (items: string[]) => void; placeholder: string }) { const add = () => onChange([...items, ""]); const update = (index: number, value: string) => onChange(items.map((item, i) => i === index ? value : item)); const remove = (index: number) => onChange(items.filter((_, i) => i !== index)); return <div className="space-y-3"><div className="flex items-center justify-between gap-3"><label className="font-bold">{label}</label><TusanButton type="button" variant="secondary" onClick={add}>+ افزودن</TusanButton></div>{items.length === 0 && <p className="text-sm text-[var(--muted)]">موردی اضافه نشده است.</p>}{items.map((item, index) => <div key={index} className="flex gap-2"><TusanInput value={item} onChange={e => update(index, e.target.value)} placeholder={placeholder} /><TusanButton type="button" variant="secondary" onClick={() => remove(index)}>حذف</TusanButton></div>)}</div>; }
export default function StructuredSeoContentEditor({ value, onChange }: { value: ServiceSeoContentData | null | undefined; onChange: (value: ServiceSeoContentData) => void }) { const [open, setOpen] = useState(true); const data: ServiceSeoContentData = { ...emptyServiceSeoContent(), ...(value || {}), steps: value?.steps || [], requirements: value?.requirements || [], notes: value?.notes || [], faq: value?.faq || [] }; const patch = (next: Partial<ServiceSeoContentData>) => onChange({ ...data, ...next }); const updateFaq = (index: number, field: "question" | "answer", text: string) => patch({ faq: data.faq!.map((item, i) => i === index ? { ...item, [field]: text } : item) }); return <TusanCard className="p-5 space-y-5"><div className="flex items-start justify-between gap-4"><div><h3 className="font-black text-lg">محتوای ساختاریافته SEO</h3><p className="text-sm text-[var(--muted)] mt-1">محتوای قابل نمایش در صفحه خدمت و مناسب برای ساختاردهی بهتر محتوای SEO.</p></div><TusanButton type="button" variant="secondary" onClick={() => setOpen(v => !v)}>{open ? "بستن" : "نمایش"}</TusanButton></div>{open && <div className="space-y-6"><div><label className="block font-bold mb-2">مقدمه</label><textarea value={data.intro || ""} onChange={e => patch({ intro: e.target.value })} rows={4} className="w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-4 py-3 outline-none" placeholder="یک معرفی کوتاه و مفید از خدمت..." /></div><div><label className="block font-bold mb-2">متن اصلی</label><textarea value={data.body || ""} onChange={e => patch({ body: e.target.value })} rows={7} className="w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-4 py-3 outline-none" placeholder="توضیحات کامل و کاربردی خدمت..." /></div><ListEditor label="مراحل انجام کار" items={data.steps || []} onChange={steps => patch({ steps })} placeholder="مرحله انجام خدمت" /><ListEditor label="مدارک و اطلاعات موردنیاز" items={data.requirements || []} onChange={requirements => patch({ requirements })} placeholder="مدرک یا اطلاعات موردنیاز" /><ListEditor label="نکات مهم" items={data.notes || []} onChange={notes => patch({ notes })} placeholder="نکته مهم برای مشتری" /><div className="space-y-3"><div className="flex items-center justify-between gap-3"><div><label className="font-bold">سؤالات متداول</label><p className="text-xs text-[var(--muted)] mt-1">پرسش و پاسخ‌های واقعی برای محتوای صفحه خدمت.</p></div><TusanButton type="button" variant="secondary" onClick={() => patch({ faq: [...(data.faq || []), { question: "", answer: "" }] })}>+ افزودن FAQ</TusanButton></div>{(data.faq || []).map((item, index) => <div key={index} className="rounded-xl border border-[var(--border)] p-4 space-y-3"><div className="flex justify-between items-center gap-3"><span className="font-bold">FAQ {index + 1}</span><TusanButton type="button" variant="secondary" onClick={() => patch({ faq: data.faq!.filter((_, i) => i !== index) })}>حذف</TusanButton></div><TusanInput value={item.question} onChange={e => updateFaq(index, "question", e.target.value)} placeholder="سؤال متداول مشتری" /><textarea value={item.answer} onChange={e => updateFaq(index, "answer", e.target.value)} rows={4} className="w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-4 py-3 outline-none" placeholder="پاسخ کامل و دقیق..." /></div>)}</div></div>}</TusanCard>; }
