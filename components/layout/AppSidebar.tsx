"use client";
import Link from "next/link";import {usePathname,useRouter} from "next/navigation";import {useEffect,useState} from "react";import {supabase} from "@/lib/supabase";import {GlassPanel,TusanButton} from "@/components/ui";
type UserRole="guest"|"user"|"admin"|"order_manager"|"support_operator"|"both";
export default function AppSidebar({mobile=false,onClose}:{mobile?:boolean;onClose?:()=>void}){const pathname=usePathname();const router=useRouter();const[role,setRole]=useState<UserRole>("guest");const[email,setEmail]=useState("");const[fullName,setFullName]=useState("");useEffect(()=>{void loadUser()},[]);async function loadUser(){const{data:{user}}=await supabase.auth.getUser();if(!user){setRole("guest");return}setEmail(user.email||"");const[{data:profile},{data:assignments}]=await Promise.all([supabase.from("profiles").select("role,full_name").eq("id",user.id).maybeSingle(),supabase.from("staff_role_assignments").select("staff_roles(code)").eq("user_id",user.id).eq("status","approved")]);setFullName(profile?.full_name||"");if(profile?.role==="admin"){setRole("admin");return}const codes=(assignments||[]).flatMap((a:any)=>Array.isArray(a.staff_roles)?a.staff_roles.map((x:any)=>x.code):a.staff_roles?.code?[a.staff_roles.code]:[]);const manager=codes.includes("order_manager"),operator=codes.includes("support_operator");setRole(manager&&operator?"both":manager?"order_manager":operator?"support_operator":"user")}
async function handleLogout(){await supabase.auth.signOut();router.push("/auth?mode=login");router.refresh();onClose?.()}
const publicItems=[{href:"/",label:"خانه",icon:"🏠"},{href:"/services",label:"خدمات",icon:"📋"},{href:"/orders",label:"پیگیری سفارش",icon:"🔎"}];const userItems=[{href:"/dashboard",label:"داشبورد",icon:"📊"},{href:"/profile",label:"پروفایل",icon:"👤"},{href:"/orders",label:"سفارش‌های من",icon:"🧾"},{href:"/messages",label:"پیام‌ها",icon:"💬"}];const managerItems=[{href:"/manager/orders",label:"داشبورد مدیر سفارشات",icon:"📦"},{href:"/manager/finance",label:"درآمد و تسویه حساب",icon:"💰"},{href:"/manager/support",label:"پشتیبانی سفارش‌ها",icon:"💬"}];const operatorItems=[{href:"/operator/support",label:"داشبورد اپراتور پشتیبانی",icon:"🎧"}];const adminItems=[{href:"/admin",label:"پنل مدیریت",icon:"🛠️"},{href:"/admin/orders",label:"مدیریت سفارش‌ها",icon:"📦"},{href:"/admin/support",label:"پشتیبانی آنلاین",icon:"💬"},{href:"/admin/staff",label:"اعطای دسترسی",icon:"👥"},{href:"/admin/services",label:"مدیریت خدمات",icon:"⚙️"},{href:"/admin/announcements",label:"اطلاعیه‌ها",icon:"📢"},{href:"/admin/reports",label:"گزارش‌ها",icon:"📈"},{href:"/admin/settings",label:"تنظیمات سایت",icon:"⚙️"},{href:"/admin/appearance",label:"ظاهر سایت",icon:"🎨"}];function Item({href,label,icon}:{href:string;label:string;icon:string}){const active=pathname===href||pathname.startsWith(href+"/");return <Link href={href} onClick={()=>onClose?.()} className={`flex items-center gap-3 rounded-xl px-3 py-3 transition ${active?"bg-[var(--primary)] text-white":"text-[var(--text)] hover:bg-[var(--surface-secondary)]"}`}><span className="text-lg">{icon}</span><span className="font-bold">{label}</span></Link>}
return <GlassPanel className={`h-full flex flex-col ${mobile?"rounded-none border-0":"rounded-3xl"}`}><div className="p-5 border-b border-[var(--border)]"><div className="text-2xl font-black text-[var(--text)]">توسن</div>{role==="guest"?<div className="mt-3"><Link href="/auth?mode=login" onClick={()=>onClose?.()}><TusanButton className="w-full">ورود / ثبت‌نام</TusanButton></Link></div>:<div className="mt-3"><div className="font-bold text-[var(--text)]">{fullName||"کاربر"}</div><div className="text-sm text-[var(--text-muted)] break-all">{email}</div></div>}</div><div className="flex-1 overflow-y-auto p-4 space-y-6"><div><div className="mb-2 text-xs font-bold text-[var(--text-muted)]">عمومی</div><div className="space-y-1">{publicItems.map(i=><Item key={i.href}{...i}/>)}</div></div>{role!=="guest"&&<div><div className="mb-2 text-xs font-bold text-[var(--text-muted)]">حساب کاربری</div><div className="space-y-1">{userItems.map(i=><Item key={i.href}{...i}/>)}</div></div>}{(role==="order_manager"||role==="both")&&<div><div className="mb-2 text-xs font-bold text-[var(--text-muted)]">مدیریت سفارشات</div><div className="space-y-1">{managerItems.map(i=><Item key={i.href}{...i}/>)}</div></div>}{(role==="support_operator"||role==="both")&&<div><div className="mb-2 text-xs font-bold text-[var(--text-muted)]">پشتیبانی</div><div className="space-y-1">{operatorItems.map(i=><Item key={i.href}{...i}/>)}</div></div>}{role==="admin"&&<div><div className="mb-2 text-xs font-bold text-[var(--text-muted)]">مدیریت</div><div className="space-y-1">{adminItems.map(i=><Item key={i.href}{...i}/>)}</div></div>}</div>{role!=="guest"&&<div className="p-4 border-t border-[var(--border)]"><TusanButton variant="secondary" className="w-full" onClick={handleLogout}>خروج از حساب</TusanButton></div>}</GlassPanel>}
