import { NextResponse, type NextRequest } from "next/server";
import { supabaseAdmin } from "@/lib/supabaseAdmin";
import { checkRateLimit } from "@/lib/security/rateLimit";
export const runtime="nodejs";
const MAX_FILE_SIZE=10*1024*1024,MAX_MULTIPART_BODY_SIZE=11*1024*1024,MAX_TITLE_LENGTH=200,MAX_FILES_PER_ORDER=20;
const ALLOWED_EXTENSIONS=new Set(["pdf","jpg","jpeg","png","webp","json"]),ALLOWED_MIME_TYPES=new Set(["application/pdf","image/jpeg","image/png","image/webp","application/json","application/octet-stream"]);
function badRequest(message:string,status=400){return NextResponse.json({error:message},{status})}function extensionOf(name:string){const i=name.lastIndexOf(".");return i>=0?name.slice(i+1).toLowerCase():""}function detectMime(bytes:Uint8Array){if(bytes.length>=5&&new TextDecoder().decode(bytes.slice(0,5))==="%PDF-")return"application/pdf";if(bytes.length>=3&&bytes[0]===255&&bytes[1]===216&&bytes[2]===255)return"image/jpeg";if(bytes.length>=8&&bytes[0]===137&&bytes[1]===80&&bytes[2]===78&&bytes[3]===71&&bytes[4]===13&&bytes[5]===10&&bytes[6]===26&&bytes[7]===10)return"image/png";if(bytes.length>=12&&bytes[0]===82&&bytes[1]===73&&bytes[2]===70&&bytes[3]===70&&bytes[8]===87&&bytes[9]===69&&bytes[10]===66&&bytes[11]===80)return"image/webp";return null}function isValidJson(text:string){try{JSON.parse(text);return true}catch{return false}}
async function validateContent(file:File,bytes:Uint8Array,ext:string){const mime=detectMime(bytes);if(ext==="pdf")return mime==="application/pdf";if(ext==="jpg"||ext==="jpeg")return mime==="image/jpeg";if(ext==="png")return mime==="image/png";if(ext==="webp")return mime==="image/webp";if(ext==="json")return(file.type==="application/json"||file.type==="application/octet-stream")&&isValidJson(await file.text());return false}
export async function POST(request:NextRequest){try{const len=request.headers.get("content-length");if(len){const n=Number(len);if(!Number.isFinite(n)||n<0)return badRequest("اندازه درخواست معتبر نیست.");if(n>MAX_MULTIPART_BODY_SIZE)return badRequest("حجم درخواست بیش از حد مجاز است.",413)}const auth=request.headers.get("authorization")||"",token=auth.startsWith("Bearer ")?auth.slice(7).trim():"";if(!token)return badRequest("احراز هویت الزامی است.",401);const admin=supabaseAdmin(),{data:{user},error:authError}=await admin.auth.getUser(token);if(authError||!user)return badRequest("نشست کاربر معتبر نیست.",401);const rate=await checkRateLimit({scope:"orders:upload-file",request,userId:user.id,limit:5,windowSeconds:60,ipMultiplier:3});if(rate)return rate;const fd=await request.formData(),orderId=String(fd.get("orderId")||"").trim(),title=String(fd.get("fileTitle")||"").trim(),fileValue=fd.get("file");if(!orderId)return badRequest("شناسه سفارش معتبر نیست.");if(!title)return badRequest("عنوان مدرک الزامی است.");if(title.length>MAX_TITLE_LENGTH)return badRequest("عنوان مدرک بیش از حد مجاز است.");if(!(fileValue instanceof File))return badRequest("فایل ارسال نشده است.");if(fileValue.size<=0)return badRequest("فایل خالی است.");if(fileValue.size>MAX_FILE_SIZE)return badRequest("حجم فایل بیش از ۱۰ مگابایت است.");const ext=extensionOf(fileValue.name);if(!ALLOWED_EXTENSIONS.has(ext)||!ALLOWED_MIME_TYPES.has(fileValue.type||"application/octet-stream"))return badRequest("نوع فایل مجاز نیست.");const bytes=new Uint8Array(await fileValue.arrayBuffer());if(!(await validateContent(fileValue,bytes,ext)))return badRequest("محتوای واقعی فایل با نوع اعلام‌شده مطابقت ندارد.");const{data:order,error:orderError}=await admin.from("orders").select("id,user_id,assigned_staff_id,status,processing_status").eq("id",orderId).maybeSingle();if(orderError||!order)return badRequest("سفارش پیدا نشد.",404);const{data:profile}=await admin.from("profiles").select("role").eq("id",user.id).maybeSingle();const isAdmin=profile?.role==="admin",isAssignedStaff=order.assigned_staff_id===user.id;if(order.user_id!==user.id&&!isAdmin&&!isAssignedStaff)return badRequest("دسترسی به این سفارش مجاز نیست.",403);if(order.status==="cancelled"||order.status==="completed"||order.processing_status==="completed")return badRequest("امکان ارسال مدرک برای این سفارش وجود ندارد.",409);const{count,error:countError}=await admin.from("order_files").select("id",{count:"exact",head:true}).eq("order_id",orderId);if(countError)return badRequest("امکان بررسی تعداد مدارک سفارش وجود ندارد.",500);if((count||0)>=MAX_FILES_PER_ORDER)return badRequest("تعداد مدارک این سفارش به حداکثر مجاز رسیده است.",409);const safeExt=ext==="jpeg"?"jpg":ext,filePath=`${orderId}/${crypto.randomUUID()}.${safeExt}`,detectedMime=detectMime(bytes)||"application/json";const{error:uploadError}=await admin.storage.from("order-files").upload(filePath,fileValue,{contentType:detectedMime,upsert:false});if(uploadError)return badRequest("ارسال فایل انجام نشد.",500);const{error:insertError}=await admin.from("order_files").insert({order_id:orderId,file_title:title,file_name:fileValue.name,file_path:filePath,file_type:detectedMime,file_size:fileValue.size,uploaded_by:user.id});if(insertError){await admin.storage.from("order-files").remove([filePath]);return badRequest("ثبت اطلاعات فایل انجام نشد.",500)}return NextResponse.json({success:true,filePath})}catch(error){console.error("Secure order file upload error:",error);return badRequest("خطایی هنگام ارسال فایل رخ داد.",500)}}
